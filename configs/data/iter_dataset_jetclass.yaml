_target_: gabbro.data.iterable_dataset_jetclass.IterableDatamodule

# ---------

defaults:
  - defaults.yaml

# ---------
#
data_dir:/global/cfs/cdirs/m2612/treasure/datasets/Event_level_dataset/H5_maker_ZeroBias_2016G

dataset_kwargs_common:
  feature_dict: null
  # part_pt: {multiply_by: 0.3, subtract_by: 2.7, func: np.log, inv_func: np.exp}
  # part_eta: {multiply_by: 4}
  # part_phi: {multiply_by: 4}
  n_files_at_once: 5
  labels_to_load:
    - label_DATA
dataset_kwargs_train:
  files_dict:
    DATA:
      - ${data.data_dir}/*.h5

dataset_kwargs_val:
  files_dict:
    DATA:
      - ${data.data_dir}/*.h5

dataset_kwargs_test:
  files_dict:
    DATA:
      - ${data.data_dir}/*.h5
